# http
Launch a server, send requests
# https
Launch a SSL server (Secure Sockets Layer)

# fs
(File System)
Sirve para trabajar con archivos y carpetas.

const fs = require('fs');

# path
Sirve para construir rutas de archivos de forma compatible con Windows, macOS y Linux.

const path = require('path')

# os
Da información sobre el sistema operativo.

const os = require('os');

Ejemplos:

console.log(os.platform());
console.log(os.totalmem());
console.log(os.hostname());

# SQL
SQL (Structured Query Language)